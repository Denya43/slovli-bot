# Словли - Wordle-подобная игра для Telegram

Бот для игры в Wordle на русском языке с возможностью настройки длины слова и управления словарем.

## Возможности

- Игра в Wordle с русскими словами
- Настраиваемая длина слова (4-9 букв)
- Управление словарем: добавление/удаление любых слов
- Статистика игр
- Административные команды

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` с переменными окружения:
```bash
# Telegram Bot Token
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ID администратора (ваш Telegram ID)
SLOVLI_ADMIN_USER_ID=123456789

# Файлы словарей
SLOVLI_WORDS_FILE=words.txt

# База данных
SLOVLI_DB_FILE=slovli.db

# Кодировка файлов словарей (опционально)
SLOVLI_WORDS_ENCODING=utf-8
```

3. Запустите бота:
```bash
python slovli_bot.py
```

## Команды

### Основные команды
- `/start` - приветствие и информация о боте
- `/new` - начать новую игру
- `/giveup` - сдаться
- `/stats` - показать статистику
- `/help` - справка

### Команды модератора (доступны модераторам и администратору)
- `/addword <слово>` - добавить слово в словарь
- `/removeword <слово>` - удалить любое слово из словаря
- `/words [длина]` - показать статистику слов
- `/myrole` - показать свою роль

### Административные команды (только для администратора)
- `/length [число]` - установить длину слова (4-9 букв)
- `/checkword <слово>` - проверить, есть ли слово в словаре
- `/addmoderator <user_id или @username>` - добавить модератора
- `/removemoderator <user_id или @username>` - удалить модератора
- `/moderators` - показать список модераторов

## Примеры использования

### Установка длины слова
```
/length 6
```

### Добавление слова
```
/addword ПРИВЕТ
```

### Удаление слова
```
/removeword ПРИВЕТ
```

### Проверка слова
```
/checkword ПРИВЕТ
```

### Управление модераторами (только администратор)
```
# Добавить модератора по ID
/addmoderator 123456789

# Добавить модератора по username (пользователь должен хотя бы раз писать боту)
/addmoderator @username
/addmoderator username

# Удалить модератора
/removemoderator 123456789
/removemoderator @username

# Список всех модераторов
/moderators
```

### Проверка своей роли
```
/myrole
```

### Просмотр статистики слов
```
/words        # общая статистика
/words 5      # статистика для 5-буквенных слов
```

## Структура файлов

- `slovli_bot.py` - основной файл бота
- `words.txt` - основной словарь (слова разной длины)
- `slovli.db` - база данных SQLite
- `requirements.txt` - зависимости Python

## База данных

Бот использует SQLite базу данных со следующими таблицами:
- `games` - текущие игры
- `stats` - статистика пользователей
- `chat_stats` - статистика чатов
- `chat_settings` - настройки чатов
- `moderators` - список модераторов
- `users` - информация о пользователях для поддержки поиска по username

## Управление словарем

Все слова хранятся в файле `words.txt`. 

**Модераторы и администратор** могут:
- Добавлять новые слова через команду `/addword`
- Удалять любые слова через команду `/removeword`
- Просматривать статистику словаря через команду `/words`

**Только администратор** может:
- Добавлять/удалять модераторов
- Проверять наличие слов через команду `/checkword`
- Изменять настройки длины слова

При добавлении/удалении слов файл автоматически пересортировывается и словари перезагружаются.

## Развертывание на VPS

### 🚀 Автоматическая установка (рекомендуется)

Самый простой способ установить бота на VPS:

```bash
# Скачиваем и запускаем скрипт установки
curl -fsSL https://raw.githubusercontent.com/yourusername/wordly-bot/main/install.sh | bash

# Или клонируем репозиторий и запускаем
git clone https://github.com/yourusername/wordly-bot.git
cd wordly-bot
chmod +x install.sh
./install.sh
```

Скрипт автоматически:
- Установит Docker и Docker Compose
- Создаст systemd сервис для автозапуска
- Настроит все необходимые файлы

### 🐳 Docker Compose (ручная установка)

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/wordly-bot.git
cd wordly-bot
```

2. **Создайте .env файл:**
```bash
cp env.example .env
nano .env  # Отредактируйте настройки
```

3. **Запустите бота:**
```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### ⚙️ Управление через Makefile

```bash
# Показать все доступные команды
make help

# Настроить .env файл
make setup-env

# Проверить настройки и запустить
make run

# Просмотр логов
make logs

# Обновление проекта
make update
```

### 🔄 Быстрый деплой

Для быстрого деплоя на удаленный сервер:

```bash
# Деплой на сервер
./deploy.sh user@your-server.com
```

### 📋 Управление сервисом

После установки бот управляется через systemd:

```bash
# Запуск
sudo systemctl start wordly-bot

# Остановка  
sudo systemctl stop wordly-bot

# Перезапуск
sudo systemctl restart wordly-bot

# Статус
sudo systemctl status wordly-bot

# Автозапуск при загрузке
sudo systemctl enable wordly-bot
```

### 🔧 Требования к серверу

- **ОС:** Ubuntu 20.04+ / Debian 10+ / CentOS 8+
- **RAM:** Минимум 512MB, рекомендуется 1GB+
- **Диск:** 2GB свободного места
- **Сеть:** Доступ к интернету для Telegram API

### 📁 Структура файлов на сервере

```
~/wordly-bot/
├── docker-compose.yml    # Конфигурация Docker
├── Dockerfile           # Образ приложения
├── .env                # Настройки (создается при установке)
├── data/               # Папка для базы данных
│   └── slovli.db      # База данных SQLite
├── wordly_bot/         # Исходный код
└── words.txt          # Словарь слов
```

## Настройка администратора

Чтобы получить ваш Telegram ID:
1. Напишите боту @userinfobot
2. Скопируйте ваш ID
3. Установите переменную `SLOVLI_ADMIN_USER_ID` в файле `.env`
